#include <stdio.h>
#include <stdlib.h>

Structure type Node
    data, whatever, the data for the node
    left, ref to Node, the left child
    right, ref to Node, the right child
EndType

int main(){
  Node* bst = (Node*)malloc(sizeof(Node));
}
